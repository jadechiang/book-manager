$(document).ready(function() {
    $.extend(,
        resizeGrid: function(gridId) {
            $(window).resize(function() {
                var width = $(".grid-body").width() - 2;
                $("#" + gridId).setGridWidth(width);
            });
        },
        reloadGrid: function(gridId, postData) {
            jQuery("#" + gridId).jqGrid("setGridParam", {
                postData: postData,
                datatype: "json",
                page: 1
            }).trigger("reloadGrid");
        },
     	clearGrid:function(gridId){
	 		$("#"+gridId).empty();
 		}
    });
});